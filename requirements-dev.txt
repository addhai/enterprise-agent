# =============================================================================
# Enterprise Agent — 开发 / 测试依赖
# 仅本地开发与 CI 使用，不参与运行时（api/rag/worker）镜像，避免镜像膨胀。
# 安装：pip install -r requirements-dev.txt
#       pre-commit install        # 注册本地钩子（push 前秒级拦截）
# =============================================================================

# 测试框架（CI 与本地 `make test` / pre-commit 共用）
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.24.0,<1.0   # 钉在 0.x：1.0 事件循环行为有变化，websocket 类测试疑在其下挂死（Run tests 卡 4.5h 主嫌疑），先回钉保稳定
pytest-xdist>=3.6.0          # 并行执行，缩短反馈时间（pytest -n auto）
pytest-timeout>=2.3.0        # 单用例超时强制失败并输出堆栈（CI 配合 --timeout=120），防任何用例挂死拖垮整个 job
pre-commit>=3.8.0           # 本地质量门禁（ruff + 快测子集）
pytest-github-actions-annotate-failures>=0.2.1  # CI 失败用例转 GitHub annotation（公开可读）；该包最新仅 0.2.x，勿写高版本号（0.9.0 曾导致 CI Install dependencies 秒挂）

# SAST（CI 安全扫描 + 本地 ci-sast）
bandit>=1.7.0
semgrep>=1.0.0
