# 容器内禁止把 localhost / 内部服务名 的请求发往宿主代理（Clash 127.0.0.1:7890）。
# Docker Desktop 的全局代理设置会注入 http_proxy 到每个容器，导致容器内
# 访问 127.0.0.1 / 服务发现名 时走代理而连接失败（healthcheck 全红、应用内部调用断链）。
# 这里把内部流量全部排除代理；外部（dashscope 等）仍走代理。
NO_PROXY=localhost,127.0.0.1,postgres,redis,rabbitmq,minio,milvus-standalone,rag-service,api-service,ws-service,frontend,apisix,apisix-dashboard,agent-net
no_proxy=localhost,127.0.0.1,postgres,redis,rabbitmq,minio,milvus-standalone,rag-service,api-service,ws-service,frontend,apisix,apisix-dashboard,agent-net
