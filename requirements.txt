# Core
langchain>=0.3.0
langgraph>=0.2.0
langsmith>=0.2.0
langchain-openai>=0.2.0
langchain-chroma>=0.2.0
langchain-community>=0.3.0

# Server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0

# Security
bcrypt>=4.0.0             # 密码哈希（替代不安全的 SHA-256）

# Data
chromadb>=0.5.0
redis>=5.0.0
psycopg2-binary>=2.9.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# RAG
rank-bm25>=0.2.0
sentence-transformers>=3.0.0
pymupdf>=1.23.0          # PDF 加载（PyMuPDF）
beautifulsoup4>=4.12.0   # HTML 正文提取
chardet>=5.0.0           # 编码检测
python-docx>=0.8.11      # DOCX 加载

# 多模态视觉管线（阿里百炼 Qwen-VL）
# openai>=1.0.0          # 百炼兼容 OpenAI 格式，需安装 openai 客户端

# Level 2: 传统 OCR（二选一，作为视觉模型不可用时的降级）
# paddleocr>=2.7.0       # 中文 OCR 优先（需 paddlepaddle）
# pytesseract>=0.3.10    # 英文 OCR 备选（需系统安装 tesseract-ocr）

# Cloud-Native (Milvus + MinIO + RabbitMQ)
pymilvus>=2.5.0           # Milvus 向量库客户端
minio>=7.2.0              # MinIO S3 对象存储客户端
pika>=1.3.0               # RabbitMQ AMQP 客户端
boto3>=1.34.0             # AWS S3 SDK (MinIO 兼容)

# Utils
python-dotenv>=1.0.0
httpx>=0.27.0
tiktoken>=0.7.0
sse-starlette
ruff>=0.9.0               # Lint & Format (替代 flake8+black)

# Protocols
zeromcp>=1.4.0             # MCP (Model Context Protocol) Server
a2a-sdk>=1.1.0             # A2A (Agent-to-Agent) 协议
